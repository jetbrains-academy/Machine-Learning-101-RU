Для визуализации понадобится библиотека pillow. Реализуйте методы `getwidth` и
`getdepth`. Дополните функцию `drawnode` для визуализации дерева.

Какие предикаты влияют на классификацию объекта как класс `Goblin`?